[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nepali-holiday-api-ephemeris-provision"
version = "1.0.0"
description = "Post-install hook: download Swiss Ephemeris .se1 files into data/ephemeris/"
dependencies = ["pyswisseph>=2.10.0"]
