fastapi>=0.135.0
uvicorn>=0.23.0
pydantic>=2.10.0
pyswisseph>=2.10.0
python-dotenv>=1.0.0

# Open Graph share-image rendering (/og-image).
# Primary path: screenshot the real दिन-चक्र chart with Playwright/Chromium
#   (after `pip install`, run `playwright install --with-deps chromium`).
#   Set OG_SCREENSHOT=false to disable and use the Pillow card instead.
# Fallback path: a Pillow card. Pillow needs libraqm for Devanagari shaping:
#   `sudo apt-get install libraqm0`.
Pillow>=11.0.0
playwright>=1.40.0

# Auth / user profiles
SQLAlchemy>=2.0.30
psycopg[binary]>=3.2.0
PyJWT>=2.9.0
cryptography>=42.0.0   # RS256 verification of Google ID tokens
bcrypt>=4.1.0
email-validator>=2.1.0
